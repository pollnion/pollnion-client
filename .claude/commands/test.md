Run the full test suite for the Pollnion client project and report the results.

Run:

```
npm test
```

After running:

- List any failing tests with their error messages
- Suggest fixes for any failures
- If all tests pass, confirm the count of passing tests

If $ARGUMENTS is provided, run only tests matching that pattern:

```
npm test -- $ARGUMENTS
```
